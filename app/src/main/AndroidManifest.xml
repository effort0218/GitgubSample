<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hjh.firebaselistexample">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FirebaseListExample">
        <activity android:name=".FragmentBundleActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity android:name=".ViewBindingActivity">

        </activity>
        <activity android:name=".FrameLayoutActivty"></activity>
        <activity android:name=".CardViewActivity" />
        <activity android:name=".TableActivity" />
        <activity android:name=".CheckBoxActivity" />
        <activity android:name=".RadioButtonActivity" />
        <activity android:name=".ViewPagerActivity" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>